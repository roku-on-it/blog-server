import { UpdateUserPassword } from 'src/module/user/input/update-user-password';

describe('UpdateUserPassword', () => {
  it('should be defined', () => {
    expect(new UpdateUserPassword()).toBeDefined();
  });
});
